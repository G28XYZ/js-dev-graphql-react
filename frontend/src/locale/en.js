import { header } from "../components/header/locale/en";
import { navigation } from "../components/navigation/locale/en";
import { note } from "../components/note/locale/en";
import { auth } from "../pages/auth/locale/en";

const common = {
  loading: "Loading...",
  error: "Error!",
};

const en = { common, header, navigation, note, auth };

export default en;
